# 🎓 IELTS Tutor RAG Agent (基于 Qwen2.5-7B 的雅思智能助教)

> **An Advanced RAG Agent for IELTS Preparation running locally.**
>
> 一个基于 Qwen2.5-7B、LangGraph 和混合检索技术构建的具备自我反思与检索优化能力的本地化雅思备考助手。实现了低资源下的 Agentic RAG。

## 💡 为什么做这个项目？

for正在备考雅思的学生，通用大模型在回答具体的评分标准（如 Band Descriptors）时经常产生幻觉，无法具备针对性的提出批改建议进行优化。通过构建这个 RAG 系统，我不仅深入实践了 Retrieval-Augmented Generation 和 Agentic AI 的用法，并且实现在有限算力下的性能优化。（将LLM给GPU,embedding、reranker给CPU，实现在3060-6G下跑通7B模型）

* **传统 RAG**：检索 -> 生成 -> 结束，是一条直线。
* **目标Agent**：检索 -> 生成 -> 检查 -> 发现不对 -> 改写问题 -> 重新检索，这是一个圆环。

## 📖 项目简介 (Introduction)

本项目旨在构建一个低成本、高隐私、专业化的雅思助教。项目基于 Ollama 本地部署的 Qwen2.5-7B 模型，经历了从“基础 RAG”到“高级 Agent”的迭代开发。

不仅实现了基于文档的知识问答，更引入了 LangGraph 状态机，使模型具备了自我反思和查询改写的能力，有效解决了传统 RAG 系统中“检索不精准”和“回答幻觉”的痛点。

## 🛠️ 技术架构演进 (Architecture Evolution)

### 第一阶段：基线系统 (Baseline RAG) `ragchat.py`

最初搭建了一个基于 LangChain + ChromaDB 的标准线性 RAG 系统。

* **线性流程 (Linear Pipeline)**: 遵循 Retrieve (检索) -> Augment (增强) -> Generate (生成) 的标准范式。
* **Prompt 工程优化**: 针对基线模型回答死板的问题，设计了 “混合模式” (Hybrid Mode) Prompt。
    * 系统能动态判断用户意图：如果是闲聊，使用模型内生知识（Internal Knowledge）；如果是专业问题，强制基于外部知识库（External Knowledge）回答。
* **向量检索**: 使用 ChromaDB 进行余弦相似度（Cosine Similarity）搜索，召回 Top-3 相关文档。

### 第二阶段：Advanced Agentic RAG `agent_rag.py`

针对基线版本中专有名词丢失以及回答逻辑单一的问题，引入了 Agentic Workflow 进行重构。

#### 1. 检索增强 (Advanced Retrieval)
为了解决纯向量检索的局限性，构建了多路召回与精排机制：

* **混合检索 (Hybrid Search)**:
    * **BM25**: 负责关键词匹配，解决“雅思特定术语（如 TR, GRA, Task 1）”在纯语义空间中容易被忽略的问题。
    * **Vector Search**: 负责语义匹配，理解用户问题的深层含义。
    * 通过 `EnsembleRetriever` 对两者结果进行加权融合。
* **重排序 (Re-ranking)**:
    * 引入 Cross-Encoder 模型（bge-reranker），像“阅卷老师”一样对召回的文档进行细粒度打分。
    * 过滤掉相关性低的噪音文档，仅保留 Top-3 高置信度片段给大模型，大幅减少幻觉。

#### 2. 智能体工作流 (Agentic Workflow with LangGraph)
放弃了单向链条，使用 LangGraph 构建了一个有环图 (Cyclic Graph) 状态机：

* **自我反思 (Self-Reflection)**: 模型生成答案后，会触发 Hallucination Check 节点，自查“我的回答是否有文档依据？”
* **查询改写 (Query Rewriting)**: 如果自查不通过，系统不会输出错误答案，而是进入 Rewrite 节点。LLM 会将用户模糊的输入优化为更适合检索的专业提问（Query Optimization），并自动触发重试。
* **闭环机制**: `Retrieve -> Generate -> Reflect -> (Rewrite -> Retrieve)` 的动态循环确保了回答的高质量。

## 🧩 技术栈 (Tech Stack)

* **LLM Runtime**: Ollama (Running Qwen2.5-7B-Int4)
* **Framework**: LangChain & LangGraph
* **Vector DB**: ChromaDB
* **Embedding**: moka-ai/m3e-base (Optimized for Chinese/English semantic matching)
* **Reranker**: BAAI/bge-reranker-base
* **Retrieval Algorithm**: BM25 + Dense Vector Search (Hybrid)

## 🚀 快速开始 (Quick Start)

### 1. 环境准备

```bash
# 克隆仓库
git clone [https://github.com/YourName/IELTS-Tutor-RAG.git](https://github.com/YourName/IELTS-Tutor-RAG.git)
cd IELTS-Tutor-RAG

# 安装依赖
pip install -r requirements.txt
